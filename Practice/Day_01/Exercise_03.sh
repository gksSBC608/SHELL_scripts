#!/bin/bash
host=$(hostname)
echo "Script is running on $host where $host is the output of the \"hostname\" command"
echo "String output format 2:-"
echo "Script is running on ${host} where ${host} is the output of the \"hostname\" command"